/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    container: (theme) => ({
      center: true,
      padding: theme('spacing.4'),
    }),
    extend: {},
  },
  plugins: [],
};
